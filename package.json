{
  "name": "tabstack",
  "version": "1.0.0",
  "private": true,
  "description": "選択したタブをMarkdown形式でまとめてコピーするChrome拡張機能",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}
